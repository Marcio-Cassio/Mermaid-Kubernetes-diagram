This repository includes a visual infrastructure diagram built with Mermaid.

To view the rendered diagram directly on GitHub:

Open the Diagram.md file.

Click the Preview tab (or the Rendered Markdown icon) in the top-right corner of the file viewer to switch from the raw code to the visual chart.
